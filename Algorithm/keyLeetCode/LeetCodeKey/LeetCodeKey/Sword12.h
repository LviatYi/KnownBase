#pragma once
#include <vector>
#include <string>

using std::vector;
using std::string;
class Sword12 {
public:
    bool exist(vector<vector<char>>& board, string word);
    bool exist2(vector<vector<char>>& board, string word);
};

